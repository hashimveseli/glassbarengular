export interface Customer {
    //Mirroring what an customer look like in the backend
    id: number;
    name: string;
    email: string;
    phonenumber: string;
    jobTitle: string;
    customerCode: string;
    imageUrl: string;
}